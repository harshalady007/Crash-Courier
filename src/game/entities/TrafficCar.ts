import Phaser from 'phaser';
export class TrafficCar { readonly sprite: Phaser.Physics.Matter.Sprite; private target=0; constructor(scene:Phaser.Scene,x:number,y:number,private points:Phaser.Math.Vector2[],private speed:number){ this.sprite=scene.matter.add.sprite(x,y,'traffic'); this.sprite.setBody({type:'rectangle',width:48,height:82}); this.sprite.setMass(35); this.sprite.setFrictionAir(0.08); } update(): void { const p=this.points[this.target]; const dx=p.x-this.sprite.x, dy=p.y-this.sprite.y; if (Math.hypot(dx,dy)<38) this.target=(this.target+1)%this.points.length; const a=Math.atan2(dy,dx); this.sprite.setRotation(a+Math.PI/2); this.sprite.setVelocity(Math.cos(a)*this.speed, Math.sin(a)*this.speed); } }
